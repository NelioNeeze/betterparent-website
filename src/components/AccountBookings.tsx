import type { NextPage } from "next";
import styles from "../styles/coaches.module.css";

const AccountBookings: NextPage = () => {

    return (
        <div className={styles.features3}>
            <p>Deine Buchungen</p>
        </div>
    );
};

export default AccountBookings;
